/**
 * @author shawn
 */

/**
 * javascript:(function(){document.body.appendChild(document.createElement('script')).src='http://localhost:3000/javascripts/bookmarklet/bootstrap.js?s='+Math.floor(Math.random()*1000);})();
 * Paste this into bookmarklet, ANOTHER TEST!
 * Third test.
 */

(function() {
	var bookmarklet_home = "<%= url_for(:action => 'index', :controller => 'bookmarklet', :only_path => false) %>";
	var load_annotations = "<%= url_for(:action => 'page', :controller => 'bookmarklet', :only_path => false) %>";
	var create_annotation= "<%= url_for(:action => 'annotate', :controller => 'bookmarklet', :only_path => false) %>";
	var parcelsSelectedStyle = "parcels_selected";
	var parcelsRejectedStyle = "parcels_rejected";
	var parcelsHighlightedStyle = "parcels_highlight";
	var parcelsInterfaceStyle = "parcels_interface";
	
<%=render :partial => 'util'%>

<%=render :partial => 'connection'%>

<%=render :partial => 'selector'%>

<%=render :partial => 'interface'%>

})();

